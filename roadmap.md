# Roadmap

- [ ] WebID-Sim: „Initialisiere..."-Hänger nach „Weiter" diagnostizieren
      (Messwerte aus DevTools + `journalctl -u webid-sim` sammeln, dann fixen).
      Fix-Kandidaten: Multi-Host-Rewrite im Proxy, JS-Response-Rewrite,
      MutationObserver-Reload im Overlay entschärfen.
